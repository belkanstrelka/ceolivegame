const uniqid = require('uniqid');

// OneTimeUA, 
// MaxRecurringUA,
// MaxValueGrowth,
// AvgP,
// COGS,
// CPA
class BusinessImprovements {
  constructor(params = {}) {
    this.id = uniqid();
  }
}

module.exports = BusinessImprovements;
